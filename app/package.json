{
  "name": "svelte-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "vite build",
    "dev": "vite dev",
    "check": "svelte-check --tsconfig ./tsconfig.json"
  },
  "type": "module",
  "dependencies": {
    "common": "file:../common"
  }
}
